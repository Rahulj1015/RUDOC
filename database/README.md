# Database Directory

This directory is reserved for persistent storage schemas, database migrations, and SQLite / PostgreSQL seed files in future milestones.

Currently, RUDOC operates with high-speed in-memory state models and JSON data feeds in `backend/data.js`.

